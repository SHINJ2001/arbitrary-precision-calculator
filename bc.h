#include "dll.h"

char get_token(char*);

void init_string(char**, dll*, dll*, dll*, dll*);

dll add(dll*, dll*, dll*, dll*);

dll subtract(dll*, dll*, dll*, dll*);

dll multiply(dll*, dll*, dll*, dll*);

dll divide(dll*, dll*);

dll modulo(dll*, dll*);

int cmp(dll, dll);

void init_floats(dll*, dll*);

dll exponent(dll*, dll*);
